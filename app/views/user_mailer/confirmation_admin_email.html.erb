<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      nouvelle commande : <%= @order.id %> <br>
      client email :<%= @user.email %><br>
      TTV : <%= @order.total_price %><br>
      products: <br>
    </p>
    <% @order.items.each do |item|%>
      <p> <%= item.title %> <br>
        individual price = <%= item.price %> €</p>
      <hr>
    <% end %>
  </body>
</html>
